#include <stdio.h>
#define L 3
#define C 3

int main() {
  int A[L][C], (*p)[C], j;

  // inicializar matriz 
    for (p = A; p < A + L; p++)
        (*p)[j] = 1;
        j + 4;
        

  return 0;
}
